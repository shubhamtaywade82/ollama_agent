#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "ollama_agent"

# Ollama client in irb: match exe/ollama_agent — apply env so cloud URL + API key work:
#   cfg = Ollama::Config.new
#   OllamaAgent::OllamaConnection.apply_env_to_config(cfg)
#   client = Ollama::Client.new(config: cfg)
# Bare Ollama::Config.new talks to localhost only; OLLAMA_AGENT_MODEL may still be a cloud tag → 404.
# ToolRuntime walkthrough: docs/TOOL_RUNTIME.md

require "irb"
IRB.start(__FILE__)
